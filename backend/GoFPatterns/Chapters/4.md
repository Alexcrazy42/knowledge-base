# Глава 4. Структурные паттерны

Структурные паттерны рассматривают вопрос о том, как из классов и объектов
образуются более крупные структуры.

Структурные паттерны
уровня класса используют наследование для составления композиций из
интерфейсов и реализаций. Простой пример — использование множественного
наследования для объединения нескольких классов в один.

Вместо композиции интерфейсов или реализаций структурные паттерны
уровня объекта компонуют объекты для получения новой функциональности.
Дополнительная гибкость в этом случае связана с возможностью
изменить композицию объектов во время выполнения, что недопустимо для
статической композиции классов.

# Паттерн Адаптер (Adapter)

Адаптер - паттерн, структурирующий классы и объекты

Преобразует интерфейс одного класса в другой интерфейс, на который рассчитаны
клиенты. Адаптер обеспечивает совместную работу классов с несовместимыми
интерфейсами, которая без него была бы невозможна.

Другие названия: Wrapper

![](../Images/adapter_1.png)

Применимость:
вы хотите использовать существующий класс, но его интерфейс не соответствует
вашим потребностям
требуется создать повторно используемый класс, который должен взаимодействовать
с заранее неизвестными или не связанными с ним классами, имеющими
несовместимые интфрейсы

(только для адаптера объектов!) нужно использовать несколько существующих
подклассов, но непрактично адаптировать их интерфейсы путем порождения
новых подклассов от каждого. в этом случае адаптер объектов может
приспосабливать интерфейс их общего родительского класса.

![](../Images/adapter_2.png)

![](../Images/adapter_3.png)


Участники:

Target (Shape) - целевой
определяет зависящий от предметной области интерфейс, которым пользуется
Client

Client (DrawingEditor) - клиент
вступает во взаимодействие с объектами, удовлетворяющими интефейсу Target

Adaptee (TextView) - адаптируемый
определяет существующий интерфейс, который нуждается в адаптации

Adapter (TextShape) - адаптер
адаптирует интерфейс Adaptee к интерфейсу Target


Адаптер класса:
адаптирует Adaptee к Target, перепоручая действия конкретному классу
Adaptee. Поэтому данный паттерн не будет работать, если мы захотим
одновременно адаптировать класс и его подклассы

позволяет адаптеру Adapter заместить некоторые операции адапртируемого
класса Adaptee, так как Adapter есть не что иное, как подкласс Adaptee

вводит только один новый объект. Чтобы добраться до адаптируемого класса,
не нужно никакого дополнительного обращения по указателю


Адаптер объектов:
позволяет одному адаптеру Adapter работать со многими адаптируемыми объектами
Adaptee, то есть с самим Adaptee и его подклассами (если такое имеются).
Адаптер может добавить новую функциональность сразу всем адаптируемым объектам

затрудняет замещение операций класса Adaptee. Для этого потребуется породить
от Adaptee подкласс и заставить Adapter ссылаться на этот подкласс, а не сам
Adaptee


# Паттерн Мост (Bridge)

Паттерн, структурирующий объекты

Отдель абстракцию от ее реализации так, чтобы то и другое можно было
изменять независимо

Наследование жестко привязывает реализацию к абстракции, что затрудняет
независимую модификацию, расширение и повторное использование абстракции
и ее реализации

Клиенты должны иметь возможность создавать объект без привязки к конкретной
реализации. Только сама реализация объекта должны зависеть от платформы,
на которой работает приложение. Поэтому в клиентском коде экземпляры объектов
должны создаваться без упоминания конкретных платформ.

Паттерн мост решает эти проблемы: абстракция объекта и ее реализация помещаются
в раздельные иерархии классов. Таким образом, существует одна иерерхия
с интерфейсами объекта (Window, IconWindow, TransientWindow) и другая
с конрнем WindowImp - для платформеннозависимых реализация. Так подкласс
XWindowImpl представляет реализацию для системы XWindow System

![](../Images/bridge_1.png)


Применимость:

требуется избежать постоянной привязки абстракции к реализации. Так,
например, бывает, когда реализация должна выбираться во время выполнения
программы

и абстракции, и реализации должны расширяться новыми подклассами. в таком
случае паттерн мост позволяет комбинировать разные абстракции и изменять
их независимо

изменения в реализации абстракии не должны отражаться на клиентах, то есть
клиентский код не должен перекомпилироваться

число классов стремительно разрастается, как на первой диаграмме из раздела
Мотивация. Это признак того, что иерархию следует разделить на две части.

Реализация должна совместно использоваться несколькими объектами (например,
на базе подсчета ссылок), и этот факт должен быть скрыт от клиента

![](../Images/bridge_2.png)

Abstraction (Window) - абстракция
определяет интерфейс абстракции
хранить ссылку на объекта типа Implementator

RefinedAbstraction (IconWindow) - уточненая абстракция
расширяет интерфейс, определенный абстракцией Abstraction

Implemenrator (WindowImpl) - реализатор
определяет интфрейс для классов реализации. Он не обязан точно соответстовать
интерфесу класса Abstraction. На самом деле оба интерфейса могут быть
совершенно различны. Обычно интерфейс класса Implementator предоставляет
только примитивные операции, а класс Abstraction определяет операции
более высокого уровня, основанные на этих примитивах

ConcreteImplementator (XWindowImpl, PMWindowImpl) - конкретный реализатор
реализует интфрейс класса Implementator и определяет его конкретную
реализацию

ОБъект Abstraction перенаправляет запросы клиента своему объекту Implementator


Аспекты реализации:

только один класс Implementator.

Паттерн абстрактная фабрика (113) может создать и сконфигурировать мост.
Для обеспечения совместной работы не связанных между собой классов
прежде всего предназначен паттерн адаптер (146). Обычно он применяется в уже готовых системах. Мост же участвует в проекте с самого начала
и призван поддержать возможность независимого изменения абстракций
и их реализаций.
