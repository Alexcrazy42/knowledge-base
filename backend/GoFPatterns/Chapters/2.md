Глава 2. Проектирование редактора документов

Внутреннее представление должно поддерживать:
1. Отслеживание физической структуры документа, то есть разбиение текста и графики на строки, колонки, таблицы и тд;
2. Генерирование визуального представления документа
3. Установление соответствия между позициями экрана и элементами внутреннего представления. Это позволит определить, что имеет в виду пользователь,
выбирая некоторый элемент визуального представления

Ограничения:
- Текст и графику следует трактовать единообразно. Интерфейс должен позволять свободно размещать текст
внутрь графики и наоборот. Не следует считать графику частным случаем текста или наоборот, поскольку
это в конечном счете приведет к появлению избыточных механизмов форматирования и манипулирования.
Одного набора механизмов достаточно и для текста и для графики.

**Рекурсивная композиция**

Рекурсивная композиция — это подход в ООП и проектировании систем, при котором объекты состоят из
других объектов того же типа, образуя древовидную или иерархическую структуру. Этот принцип
часто используется в паттернах проектирования, таких как Компоновщик (Composite).
Пример с формированием Json в **JsonConverterWithRecursiveComposition**

Конкретно по данной книге:
Представляя объектом каждый символ и каждый графический элемент документа, мы обеспечиваем
гибкость на самых нижних уровнях дизайна Lexi.
Текст и графика обрабатываются единообразно в том, что касается отображения, форматирования и вложения друг в
друга. Lexi можно расширить для поддержки новых наборов символов, не затрагивая никаких других функций.
Объектная структура Lexi точно отражает физическую структуру документа


У описанного подхода есть два важных следствия. Первое очевидно: для
объектов нужны соответствующие классы. Второе, менее очевидное, состоит
в том, что у этих классов должны быть совместимые интерфейсы, поскольку
мы хотим унифицировать работу с ними. Для обеспечения совместимости
интерфейсов в таком языке, как C++, применяется наследование.


**Глифы**

Глиф — это визуальное представление символа (буквы, цифры, знака препинания или иконки) в определённом
шрифте или графической системе. Это не сам символ, а его конкретное графическое воплощение с уникальными характеристиками:
1. Форма (контуры, засечки, толщина линий)
2. Размер (высота, ширина, кернинг)
3. Стиль (курсив, жирность, декоративные элементы)


В .NET для работы с глифами используется пространство имён System.Windows.Media:
```csharp
using System.Windows.Media;

// Получение глифов из шрифта
GlyphTypeface font = new GlyphTypeface(new Uri("C:/Fonts/arial.ttf"));
ushort glyphIndex = font.CharacterToGlyphMap['A']; // Индекс глифа для 'A'

// Создание визуального представления текста
var glyphRun = new GlyphRun(
    font,
    0,          // Размер шрифта
    false,      // Поддержка RTL
    1.0,        // Масштаб
    new[] { glyphIndex },
    new Point(0, 0),
    new[] { 10.0 }, // Ширина глифов
    null, null, null, null, null, null);
```


Базовый интерфейс класса Glyph определяется для всех объектов, которые могут присутсовать в структуре
документа. Его подклассы определяют как примитивные графические элементы (символы и озображения), так и
структурные элементы (строчки и колонки).

В базовом интерфейсе нужно отразить обязанности: внешнего представления (Draw, Bounds), обнаружение
точки воздействия (Intersects), структура (Insert, Remove, Child, Parent).

Глифы умеют рисовать себя на кране, знают сколько места занимают, а так же располагают информацией о своих
потомках и родителях.

![](../Images/glyph.png)


**Паттерн Composite (компоновщик)**
Рекурсивная композицияи подходит не только для документов. Ей можно пользоваться для представления любых
потенциально сложных иерархических структура. Паттерн компоновщик инкапсулирует сущность рекурсивной
композиции в объектов-ориентированных категориях.

![](../Images/composition_compositor.png)



**Паттерн Стратегия**
Инкапсуляция алгоритма в объект. Ключ к применению паттерна стратегия - проектирование интерфейсов стратегии
и контекста, достаточно общих для поддержки широкого диапазона алгоритмов.


Композиция с одним потомком и совместимые интерфейсы. В общем случае клиенту неизвестно, имеет ли он дело
с компонентом или его окружением (то есть родителем), особенно если окружение просто делегирует все операции
своему единственному компоненту. Но окружение может также расширять поведение компонента, выполняя дополнительные
действия либо до, либо после делегирования (а возможно, и до, и после). Окружение может также добавить компоненту
состояние.


![](../Images/gui_factory.png)
![](../Images/glyph_ierarchy.png)

**Инкапсуляция зависимостей от реализации**
Нужно отделить функциональность приложения от аппаратной платформы. Для этого например для класса Window будет инкапсулировать
функциональность окна в любой оконной системе: операции прорисовки базовых геометрических фигур, возможность свернуть и
развернуть окно, изменение собственных размеров, перерисовка своего содержимого при необходимости.

**Паттерн Bridge (мост)**

Операции пользователя: создание нового документа; открытие, сохранение и печать существующего документа; вырезание
выбранной части документа и вставка ее в другое место;
изменение шрифта и стиля выбранного текста;
измененеия форматирования текста (например, установка режима выравнивания);
завершение работы приложения и тд

![](../Images/command_ierarchy.png)

Теперь в классе MenuItem может храниться объект, инкапсулирующий запрос (рис. 2.12). Каждому объекту,
представляющему пункт меню, передается экземпляр того из подклассов Command, который соответствует этому
пункту, точно так же, как мы задаем текст, отображаемый в пункте меню.
Когда пользователь выбирает некоторый пункт меню, объект MenuItem просто вызывает операцию Execute для своего
объекта Command, чтобы он выполнил запрос. Заметим, что кнопки и другие видже ты могут пользоваться
объектами Command точно так же, как и пункты меню.


**Паттерн Command (Команда)**
Команда — это поведенческий паттерн, который инкапсулирует запрос в виде объекта, позволяя:
Параметризировать клиенты различными запросами.
Ставить запросы в очередь, логировать или отменять их.
Поддерживать отмену операций (undo/redo).

**Паттерн Iterator (итератор)**

**Паттерн Посетитель (Visitor)**
_________________________________________________________________

Итого при проектировании были использованы следующие паттерны:

компоновщик (196) для представления физической структуры документа;
1. Компоновщик (Composite, с. 196)
   «Компоновать объекты в древовидные структуры для представления иерархий „часть-целое“. Composite позволяет клиентам единообразно трактовать индивидуальные и составные объекты.»

✅ Как применяется:
Документ — это дерево:
```
Document
├── Section
│   ├── Paragraph
│   │   ├── TextRun ("Hello")
│   │   └── TextRun (bold: "World")
│   └── Table
│       ├── Row
│       │   ├── Cell → Paragraph → TextRun
│       │   └── Cell → Image
│       └── Row
├── Header
│   └── Paragraph → TextRun
└── Footer
```

💡 Зачем это нужно:
Единый интерфейс IDocumentNode с методами:
void Render(IRenderer r), void Accept(IVisitor v), IDocumentNode GetChild(int i)
Можно вызывать document.Render(pdfRenderer) — и всё дерево рекурсивно отрисуется.
Можно искать/заменять текст через обход дерева — без if (node is Paragraph) … else if (node is Table)….
🎯 Без Composite: куча switch/if повсюду → хрупкий, не расширяемый код.

____________________________________________________
стратегия (362) для возможности использования различных алгоритмов форматирования;
«Определяет семейство алгоритмов, инкапсулирует каждый из них и делает их взаимозаменяемыми. Strategy позволяет изменять алгоритмы независимо от клиентов, которые ими пользуются.»
✅ Как применяется:

Алгоритмы форматирования:
ILayoutStrategy: CalculateLayout(IDocumentNode node) → реализации: FlowLayoutStrategy, ColumnLayoutStrategy, ResponsiveLayoutStrategy

Или обработки текста:
IHyphenationStrategy: HyphenateWord(string word) → RussianHyphenator, EnglishHyphenator, NoHyphenation

💡 Зачем это нужно:
Пользователь выбирает: «многостолбцовый макет» → редактор подставляет ColumnLayoutStrategy.
Поддержка языков: при смене языка — меняется стратегия переносов.
Можно тестировать каждую стратегию отдельно.
🎯 Без Strategy: if (language == "ru") { ... } else if (language == "en") { ... } — в куче мест.

________________________________
декоратор (209) для оформления пользовательского интерфейса;
«Динамически добавляет объекту новую функциональность „обёртыванием“ его в полезную „оболочку“.»
```csharp
var button = new Button("Save");
var themedButton = new ThemeDecorator(button, "dark");          // добавляет тему
var tooltipButton = new TooltipDecorator(themedButton, "Сохранить"); // добавляет подсказку
var disabledButton = new DisableDecorator(tooltipButton);      // добавляет disabled-состояние
```

Или декорирование контента в документе:
```csharp
ITextRun text = new TextRun("Hello");
text = new BoldDecorator(text);
text = new HighlightDecorator(text, Color.Yellow);
```

💡 Зачем это нужно:
Гибкое комбинирование эффектов: жирный + подсветка + комментарий.
Не нужно наследовать BoldHighlightedCommentedTextRun — растёт экспоненциально.
Соответствует OCP (Open-Closed Principle).
🎯 Без Decorator: взрыв классов (BoldText, BoldHighlightedText, BoldCommentedText…).

______________________________________________________________
абстрактная фабрика (113) для поддержки нескольких стандартов оформления;
«Предоставляет интерфейс для создания семейств взаимосвязанных или взаимозависимых объектов, не специфицируя их конкретных классов.»
✅ Как применяется:
Поддержка стандартов оформления:
```csharp
// Интерфейс семейства
interface IDocumentStyleFactory
{
    IParagraphStyle CreateParagraphStyle();
    IHeaderStyle CreateHeaderStyle();
    ITableStyle CreateTableStyle();
}

// Реализации
class GostStyleFactory : IDocumentStyleFactory { ... }
class ApaStyleFactory : IDocumentStyleFactory { ... }
class CorporateStyleFactory : IDocumentStyleFactory { ... }

// Использование
var factory = new GostStyleFactory();
var paraStyle = factory.CreateParagraphStyle(); // → GostParagraphStyle
var headerStyle = factory.CreateHeaderStyle();  // → GostHeaderStyle
```

💡 Зачем это нужно:
При выборе шаблона «ГОСТ» — все элементы стиля согласованы.
Нельзя случайно создать GostParagraphStyle + ApaTableStyle — фабрика гарантирует согласованность.
Легко добавить новый стандарт — без изменения ядра.
🎯 Без Abstract Factory: ручное связывание стилей → ошибки согласованности.

_________________________________________________________________________
мост (184) для поддержки нескольких оконных систем;
«Отделяет абстракцию от её реализации так, чтобы и то, и другое можно было изменять независимо.»

✅ Как применяется:
Поддержка оконных систем (GUI-бэкенд):

```
          ┌──────────────┐          ┌──────────────────┐
          │  DocumentUI  │─────────▶│ IWindowSystem    │
          └──────────────┘          ├──────────────────┤
                   ▲                │ + CreateWindow() │
                   │                │ + DrawText()     │
        ┌──────────┴─────────┐      │ + HandleEvent()  │
        │                    │      └────────┬─────────┘
   ┌────▼─────┐        ┌─────▼─────┐         │
   │ RichText │        │  Editor   │   ┌─────▼──────┐   ┌─────────────┐
   └──────────┘        └───────────┘   │ Win32Impl  │   │  CocoaImpl  │
                                       └────────────┘   └─────────────┘
```

DocumentUI — высокоуровневая абстракция (логика редактора).
IWindowSystem — интерфейс для отрисовки и ввода.
Реализации: Win32WindowSystem, CocoaWindowSystem, GtkWindowSystem.
💡 Зачем это нужно:
Компиляция под Windows/macOS/Linux из одного кода.
Можно заменить Win32 → Avalonia без изменения DocumentUI.
Позволяет писать headless-режим для генерации PDF на сервере: HeadlessWindowSystem.
🎯 Без Bridge: #ifdef _WIN32, #elif __APPLE__ — спагетти-код.


_______________________________________________________________________

команда (275) для реализации отмены и повтора операций пользователя;

«Инкапсулирует запрос как объект, позволяя тем самым задавать параметры клиентов для обработки соответствующих запросов, ставить запросы в очередь или протоколировать их, а также поддерживать отмену операций.»

✅ Как применяется:
Операции пользователя:
```csharp
interface ICommand
{
    void Execute();
    void Undo();
}

class InsertTextCommand : ICommand
{
    public InsertTextCommand(Document doc, string text, int position) { ... }
    public void Execute() => doc.Insert(position, text);
    public void Undo() => doc.Delete(position, text.Length);
}

class FormatBoldCommand : ICommand { ... }
```

→ История: Stack<ICommand> history.

💡 Зачем это нужно:
Отмена/повтор: history.Pop().Undo() / redoStack.Push(cmd); cmd.Execute().
Протоколирование: сохранять команды в файл → «воспроизвести сессию».
Горячие клавиши: Ctrl+B → new FormatBoldCommand().Execute().
Макросы: запись последовательности команд.
🎯 Без Command: ручное хранение «до/после» — хрупко, не масштабируется.

_________________________________________________________________________________________

итератор (302) для обхода структур объектов;
«Предоставляет способ последовательного доступа ко всем элементам составного объекта, не раскрывая его внутреннего представления.»

✅ Как применяется:
Обход документа:

IDocumentNode.CreateIterator() → DocumentIterator
Возможные режимы:
По абзацам
По словосочетаниям
По изменённым частям (для совместного редактирования)

```csharp
foreach (var paragraph in document.IterateParagraphs())
{
    spellChecker.Check(paragraph.Text);
}
```

💡 Зачем это нужно:
Скрыть сложность дерева (Composite): клиент не знает про Table → Row → Cell → Paragraph.
Разные стратегии обхода: DFS, BFS, только видимые узлы.
Поддержка foreach в C#/Java.
🎯 Без Iterator: рекурсия в каждом клиенте → дублирование, ошибки.

_______________________________________________________________________

посетитель (379) для поддержки неизвестного заранее числа видов анализа без усложнения реализации структуры документа.

«Определяет операцию, выполняемую над объектами структуры. Visitor позволяет определить новую операцию, не изменяя классы объектов, над которыми она выполняется.»

✅ Как применяется:
Анализ и обработка документа:

```csharp
interface IDocumentVisitor
{
    void Visit(Paragraph p);
    void Visit(Table t);
    void Visit(Image i);
    void Visit(TextRun r);
}

class SpellCheckVisitor : IDocumentVisitor { ... }
class StatisticsVisitor : IDocumentVisitor
{
    public int WordCount { get; private set; }
    public void Visit(TextRun r) => WordCount += r.Text.Split().Length;
}
class PdfExportVisitor : IDocumentVisitor { ... }
```

```csharp
var stats = new StatisticsVisitor();
document.Accept(stats); // рекурсивно по дереву
Console.WriteLine($"Слов: {stats.WordCount}");
```

💡 Зачем это нужно:
Добавить новый анализ → просто написать новый Visitor.
Не нужно лезть в Paragraph, Table, Image — нарушать инкапсуляцию.
Double Dispatch: node.Accept(visitor) → visitor.Visit(this) — тип определяется динамически.
Поддержка независимых расширений: плагины могут предоставлять свои Visitor’ы.
🎯 Без Visitor: каждый новый анализ требует изменения всех классов → нарушение OCP.

___________________________________________________________________________

🧱 Как всё работает вместе: сценарий «сохранить в PDF»
Пользователь нажимает «Сохранить как PDF»
→ Выполняется new ExportToPdfCommand().Execute() (Command)

Внутри:
Создаётся PdfExportVisitor (Visitor)
Документ вызывает Accept(visitor) → рекурсивно обходит дерево (Iterator внутри Composite)
Каждый узел (Paragraph, Image) вызывает visitor.Visit(this)
PdfExportVisitor использует IRenderer → делегирует отрисовку PdfWindowSystem (Bridge)
Стили берутся из CorporateStyleFactory (Abstract Factory)
Если нужна подсветка синтаксиса — CodeBlock «обёрнут» в SyntaxHighlightDecorator (Decorator)
Макет рассчитывается через ColumnLayoutStrategy (Strategy)
👉 Все паттерны работают слаженно — и ни один не знает о других напрямую.

✅ Итог: почему это архитектура «на вырост». Потому что сразу задумываются о проблеме, и как ее решить паттернами

Добавить новый тип блока (например, MathFormula) → Реализовать IDocumentNode, добавить Visit(MathFormula) в Visitor’ы (Visitor + Composite)
Поддержка Linux  → Написать GtkWindowSystem (Bridge)
Новый стандарт — IEEE  → IeeeStyleFactory (Abstract Factory)
Отмена сложной операции  → Команда инкапсулирует всё (Command)
Плагин «доступность»  → AccessibilityVisitor (Visitor)
