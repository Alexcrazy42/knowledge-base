// ============================================================================
// main.js - точка входа. Создаём корневой компонент и монтируем в #app.
// Композиция минимальна: стор - singleton в модуле store.js, VM создаёт App.
// ============================================================================

import { createApp } from 'vue';
import App from './App.vue';
import './styles.css';

createApp(App).mount('#app');
