// Публичный API сегмента диалогов: сервис + хост + готовые модалки.

export { ask, dialogService } from './dialog-service';
export { DialogHost } from './dialog-host';
export { prompt, confirm, PromptModal, ConfirmModal } from './dialog-modals';
