// ============================================================================
// МОДЕЛЬ ПОЛЬЗОВАТЕЛЯ - порт User из BoardApp.Core.
// Статистика "у кого сколько задач" - это АГРЕГАЦИЯ пользователей и задач,
// поэтому живёт не здесь, а в features/manage-users (правило FSD:
// сущность не импортирует соседние сущности ради вычислений).
// ============================================================================

import { uid } from '@/shared/lib';

export interface User {
    id: string;
    name: string;
}

export const makeUser = (name: string): User => ({ id: uid(), name });
