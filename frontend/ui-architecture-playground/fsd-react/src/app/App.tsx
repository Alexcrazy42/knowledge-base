// ============================================================================
// App - корневой компонент (слой app, самый верх FSD).
// Здесь только глобальная композиция: хост диалогов + страница.
// Никакой бизнес-логики: ей не место выше страниц.
// ============================================================================

import { DialogHost } from '@/shared/ui';
import { BoardPage } from '@/pages/board';

export function App() {
    return (
        <>
            {/* Единственное место монтирования модальных окон (см. shared/ui/dialog). */}
            <DialogHost />
            <BoardPage />
        </>
    );
}
