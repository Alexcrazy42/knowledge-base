# JavaScript

## Что JavaScript делает на странице

Когда вы загружаете страничку в браузере, вы запускаете ваш код (HTML, CSS и JavaScript) внутри исполняемой среды (внутри вкладки браузера). Это как будто фабрика берёт сырьё (некий код) и выдаёт продукцию (веб-страничку).

Код JavaScript выполняется JavaScript-движком браузера, после того как код HTML и CSS был обработан и сформирован в веб-страницу. Это гарантирует, что структура и стиль страницы уже сформированы к моменту запуска JavaScript.

Это хорошо, так как часто использование JavaScript заключается в динамическом изменении HTML и CSS в целях обновления пользовательского интерфейса посредством Document Object Model API (как упоминалось выше). Если бы запуск JavaScript осуществлялся прежде загрузки HTML и CSS, то это привело бы к возникновению ошибок.

### Безопасность браузера

Каждая вкладка является изолированной средой исполнения. Из одной вкладки мы не имеем доступа к информации на другой. Это мера безопасности

### ПРИМЕЧАНИЕ:  есть способы отправлять код и данные между разными веб-сайтами/вкладками безопасным способом


### Последовательность выполнение JavaScript

JavaScript является интерпретируемым языком — код запускается сверху вниз и результат запуска немедленно возвращается.

## События

Браузер регистрирует обработчики событий для элементов DOM-дерева с помощью JavaScript, который добавляется разработчиком. Когда происходит событие, браузер вызывает соответствующий обработчик событий, который выполняет необходимые действия.

Браузер использует цикл событий (event loop) для обработки событий. Цикл событий - это механизм, который постоянно проверяет очередь событий и вызывает соответствующие обработчики событий, когда события попадают в очередь. Цикл событий позволяет браузеру обрабатывать множество событий одновременно, не блокируя при этом интерфейс пользователя.

Кроме того, браузеры предоставляют множество API интерфейсов, которые позволяют разработчикам регистрировать обработчики событий для различных типов событий, таких как нажатие клавиш, прокрутка страницы, изменение размера окна браузера и т.д. Эти API интерфейсы также используют модель событий, основанную на архитектуре DOM.

## Объекты

Объект - это набор связанных функций, хранящихся в одной группе

В JavaScript объект - это сущность, которая может содержать свойства и методы. Объект представляет собой коллекцию пар "имя-значение", где имя - это строка, а значение - это любое значение JavaScript, включая другие объекты.

Объекты в JavaScript являются динамическими и гибкими. Вы можете добавлять, удалять и изменять свойства объекта в любое время. Объекты также могут наследовать свойства и методы от других объектов, что позволяет создавать иерархии объектов.

Объекты в JavaScript используются для представления различных сущностей, таких как HTML-элементы, даты, массивы, функции и т.д. Объекты также используются для создания пользовательских типов данных и для организации кода в модулях и библиотеках.

Литералы объекта (object literal) — мы буквально вписали все содержимое объекта для его создания. Этот способ сильно отличается от объектов реализованных классами, которые мы рассмотрим позже.

Можно использовать как точеченую запись, так и скобочную, также есть понятие внутренних имен

## Прототип объектов

Прототипы - это механизм, с помощью которого объекты JavaScript наследуют свойства друг от друга.

В JavaScript каждый объект имеет свойство __proto__, которое ссылается на его прототип. Когда мы пытаемся получить доступ к свойству или методу объекта, который не определен в этом объекте, JavaScript ищет это свойство или метод в прототипе объекта. Если свойство или метод не найдены в прототипе, JavaScript проверяет прототип прототипа и так далее, пока не дойдет до объекта Object, который является прототипом для всех объектов в JavaScript.

## Синхронный JS

Большая часть функциональности, которую мы рассматривали в предыдущих обучающих модулях, является синхронной — вы запускаете какой-то код, а результат возвращается, как только браузер может его вернуть.

Пока выполняется каждая операция, ничего больше не может произойти — обработка (отображение) документа приостановлена. Так происходит, потому что JavaScript является однопоточным. В каждый момент времени может выполняться только одна команда, обрабатываемая в единственном — главном потоке. Все остальные действия блокируются до окончания выполнения текущей команды.

## Асинхронный JavaScript

По причинам, упомянутым ранее (например, относящимся к блокировке), множество Web API особенностей теперь используют асинхронный код, особенно те,что имеют доступ к внешним устройствам или получают от них некоторые ресурсы, такие как получение файла из сети, запрос к базе данных и получение данных из базы, доступ к потоковому видео на веб-камере, просмотр дисплея на гарнитуре виртуальной реальности.

Есть два типа стиля асинхронного кода, с которыми вы столкнётесь в коде JavaScript, старый метод — колбэки (callbacks) и более новый — промисы (promises). В следующих разделах мы познакомимся с каждым из них.

### Асинхронные колбэки

Асинхронные колбэки — это функции, которые определяются как аргументы при вызове функции, которая начнёт выполнение кода на заднем фоне. Когда код на заднем фоне завершает свою работу, он вызывает колбэк-функцию, оповещающую, что работа сделана, либо оповещающую о трудностях в завершении работы. Обратные вызовы — немного устаревшая практика, но они все ещё употребляются в некоторых старомодных, но часто используемых API.

Пример асинхронного колбэка вторым параметром addEventListener() (как мы видели выше):

```
btn.addEventListener("click", () => {
  alert("You clicked me!");

  let pElem = document.createElement("p");
  pElem.textContent = "This is a newly-added paragraph.";
  document.body.appendChild(pElem);
});
```

Первый параметр — тип обрабатываемого события, второй параметр — колбэк-функция, вызываемая при срабатывании события.

При передаче колбэк-функции как аргумента в другую функцию, мы передаём только ссылку на функцию как аргумент, следовательно колбэк-функция не выполняется мгновенно. Она вызывается асинхронно внутри тела, содержащего функцию. Эта функция должна выполнять колбэк-функцию в нужный момент.

Вы можете написать свою собственную функцию, содержащую колбэк-функцию. Давайте взглянем на ещё один пример, в котором происходит загрузка ресурсов через XMLHttpRequest API:

```
function loadAsset(url, type, callback) {
  const xhr = new XMLHttpRequest();
  xhr.open("GET", url);
  xhr.responseType = type;

  xhr.onload = function () {
    callback(xhr.response);
  };

  xhr.send();
}

function displayImage(blob) {
  const objectURL = URL.createObjectURL(blob);

  const image = document.createElement("img");
  image.src = objectURL;
  document.body.appendChild(image);
}

loadAsset("coffee.jpg", "blob", displayImage);
```

Колбэк-функции универсальны — они не только позволяют вам контролировать порядок, в котором запускаются функции и данные, передающиеся между ними, они также позволяют передавать данные различным функциям, в зависимости от обстоятельств. Вы можете выполнять различные действия с загруженным ответом, такие как processJSON(), displayText(), и другие.

### ВНИМАНИЕ: не все колбэк-функции асинхронны — некоторые запускаются синхронно

### Промисы

Промисы — новый стиль написания асинхронного кода, который используется в современных Web API. Хорошим примером является fetch() API, который современнее и эффективнее чем XMLHttpRequest. Посмотрим на краткий пример:

```
fetch("products.json")
  .then((response) => {
    if (!response.ok) {
      throw new Error(`HTTP error: ${response.status}`);
    }
    return response.json();
  })
  .then((json) => initialize(json))
  .catch((err) => console.error(`Fetch problem: ${err.message}`));
```

В примере видно, как fetch() принимает один параметр — URL ресурса, который нужно получить из сети, — и возвращает промис. Промис — это объект, представляющий асинхронную операцию, выполненную удачно или неудачно. Он представляет собой как бы промежуточное состояние. По сути, это способ браузера сказать: "я обещаю вернуться к вам с ответом как можно скорее", поэтому в дословном переводе "промис" (promise) означает "обещание".

### Очередь событий

Асинхронные операции, такие как промисы, помещаются в очередь событий, которая запускается после завершения обработки основного потока, чтобы они не блокировали выполнение JavaScript-кода. Поставленные в очередь операции завершатся как можно скорее, а затем вернут свои результаты в среду JavaScript.

### Промисы и колбэк-функции

Промисы имеют некоторое сходство со старомодными колбэк-функциями. По сути, они являются возвращаемым объектом, к которому вы присоединяете колбэк-функции, вместо того, чтобы передавать колбэки в функцию.

Тем не менее, промисы сделаны специально для обработки асинхронных операций, и имеют много преимуществ по сравнению с колбэками:

1. Вы можете объединить несколько асинхронных операций вместе, используя несколько операций.then(), передавая результат одного в следующий в качестве входных данных. Это гораздо сложнее сделать с колбэками, которые часто заканчиваются массивным «адом колбэков»

2. Обратные вызовы Promise всегда вызываются в строгом порядке, который они помещают в очередь событий.

3. Обработка ошибок намного лучше — все ошибки обрабатываются одним блоком catch в конце блока, а не обрабатываются индивидуально на каждом уровне «пирамиды»

4. Промисы избегают инверсии управления, в отличие от колбэков, которые теряют полный контроль над тем, как будет выполняться функция при передаче колбэка в стороннюю библиотеку.

# Природа асинхронного кода
В своей основной форме JavaScript является синхронным, блокирующим, однопоточным языком, в котором одновременно может выполняться только одна операция. Но веб-браузеры определяют функции и API, которые позволяют нам регистрировать функции, которые не должны выполняться синхронно, а должны вызываться асинхронно, когда происходит какое-либо событие (время, взаимодействие пользователя с мышью или получение данных по сети, например). Это означает, что вы можете позволить своему коду делать несколько вещей одновременно, не останавливая и не блокируя основной поток.

## Основы async/await

Ключевое слово async позволяет сделать из обычной функции асинхронную функцию. Такая функция делает две вещи: 
- Оборачивает возвращаемое значение в Promise 
- Позволяет использовать ключевое слово await

Асинхронные функции становятся по настоящему мощными, когда вы используете ключевое слово await — по факту, await работает только в асинхронных функциях. Мы можем использовать await перед promise-based функцией, чтобы остановить поток выполнения и дождаться результата её выполнения (результат Promise). В то же время, остальной код нашего приложения не блокируется и продолжает работать.

Вы можете использовать await перед любой функцией, которая возвращает Promise, включая Browser API функции.

### А как это работает:

Вы могли заметить, что мы обернули наш код в функцию и сделали её асинхронной с помощью async. Это было обязательно – нам надо создать контейнер, внутри которого будет запускаться асинхронный код, и будет возможность дождаться его результата с помощью await, не блокируя остальной код нашего скрипта.

Внутри myFetchImage() находится код, который слегка напоминает версию на Promise, но есть важные отличия. Вместо того, чтобы писать цепочку блоков .then() мы просто использует ключевое слово await перед вызовом promise-based функции и присваиваем результат в переменную. Ключевое слово await говорит JavaScript runtime приостановить код в этой строке, не блокируя остальной код скрипта за пределами асинхронной функции. Когда вызов promise-based функции будет готов вернуть результат, выполнение продолжится с этой строки дальше.

# Workers

There are three different sorts of workers:

1. dedicated workers - it's used by a single script instance
2. shared workers - can be shared by several different scripts running in different windows
3. service workers - act like proxy servers, caching resources so that web applications can work when the user is offline. They're a key component of Progressive Web Apps.


# Client-side Web API 

Client-side Web API в JavaScript — это набор интерфейсов и методов, предоставляемых браузером, которые позволяют веб-разработчикам взаимодействовать с различными функциями и возможностями браузера и устройства. Эти API предоставляют доступ к таким функциям, как работа с DOM, сетевые запросы, хранилище данных, мультимедиа, геолокация и многое другое.

Основные категории Client-side Web API:

1. DOM (Document Object Model) API
2. Fetch API
3. Web Storage API
4. Geolocation API
5. Web Workers API
6. Canvas API
7. WebRTC API
8. Service Workers API
9. WebSocket API
10. File API
11. Drag and Drop API
12. Notification API / Vibration API
13. IndexedDB API
14. Web Audio API
15. WebVR API

## Управление документами

### Основные части браузера 

1. Окно - это вкладка браузера, в которую загружается веб-страница; это представлено в JavaScript объектом Window.

2. Навигатор представляет состояние и идентификатор браузера (т. е. пользовательский агент), как он существует в Интернете. В JavaScript это представлено объектом Navigator

3. Документ (представленный DOM в браузерах) представляет собой фактическую страницу, загруженную в окно, и представлен в JavaScript объектом Document.

### Объектная модель документа

**Получение элементов:** getElementById, getElementsByClassName, getElementsByTagName, querySelector, querySelectorAll

**Манипуляция с элементами:** изменение текста, изменение html содержимого, изменении атрибутов, изменение стиля, добавление и удаление классов, создание новых элементов, перемещение и удаление элементов

**Обработка событий**

1. События UI: загрузка страницы целиком, изменение размера окна, прокручивание, покидание страницы

2. События формы: отправка формы, изменение значений, при вводе текста, потеря фокуса

3. События мыши: клик, двойной клик, вход/выход из элемента, движение внутри элемента, нажатие/отпускании кнопки мыши

4. События клавиатуры: нажатие и отпускание


## Client-side Storage

1. Cookies (Куки)

Маленькие кусочки данных. Они используются для хранения информации о сеансах, пользовательских предпочтениях и идентификации пользователей. Обычно ограничены по размеру (до 4 КБ на домен) и по числу (обычно несколько сотен куки на домен). Куки отправляются на сервер с каждым HTTP-запросом. Сохранение данных сеансов, настроек пользователей, трекинг активности, аутентификация.

2. Local Storage (Локальное хранилище)

Хранилище, которое позволяет сохранять данные в браузере без срока истечения. Это более мощное хранилище по сравнению с куки и имеет большие ограничения по размеру. Обычно до 5-10 МБ на домен. Данные хранятся в виде пар "ключ-значение" и не отправляются на сервер с каждым запросом. : Хранение пользовательских данных, настроек, кэша и других данных, которые должны быть доступны между сеансами.

3. Session Storage (Сессионное хранилище)

Похожее на Local Storage, но данные хранятся только до тех пор, пока открыта текущая вкладка или окно браузера. После закрытия вкладки данные удаляются. Обычно до 5 МБ на домен, данные хранятся только в рамках текущей сессии. Хранение временных данных, таких как состояние формы, которые не нужно сохранять после закрытия вкладки. 

4. IndexedDB

Асинхронная база данных, которая позволяет хранить структурированные данные в виде объектов и поддерживает индексацию для быстрой выборки. Это более сложное хранилище, которое поддерживает транзакции и запросы. Размер хранилища зависит от браузера и настроек пользователя. Обычно позволяет хранить большие объемы данных. Хранение сложных данных, таких как результаты поисков, данные приложений, кэширование больших объемов информации.

5. WebSQL (устаревший)

SQL-совместимое хранилище, которое позволило бы создавать и использовать базы данных SQL в браузере. Однако поддержка WebSQL была прекращена и его использование не рекомендуется.

6. Cache API

Предназначен для хранения ресурсов, таких как HTML, CSS, JavaScript и изображения, в кэше для офлайн-доступа и ускорения загрузки. Используется в сочетании с Service Workers. Размер хранилища зависит от браузера и доступного пространства. Кэширование ресурсов для работы приложений в офлайн-режиме, улучшение производительности.


# Что нужно сделать:
СДЕЛАТЬ ЗЛОЙ ШАР!!!!

СДЕЛАТЬ ЧТОБЫ ШАРЫ ПРИ СТОЛКНОВЕНИИ ОТЛЕТАЛИ ДРУГ ОТ ДРУГА

Посмотреть с графикой (three.js)

Сделать что нибудь с 3rd-party-api

Sequencing_animations

Event Targets - непонятно, как сделать так, допустим, чтобы мы провели мимо какой либо части экрана, и это событие засчиталось




https://learn.javascript.ru - после читаю это

https://habr.com/ru/articles/486820/ - подготовка к собесу




